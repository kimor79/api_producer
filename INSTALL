api_producer requires the following:

* PHP (tested with 5.3)
  * json
  * pcre
* A webserver (tested with Apache 2.2)

Copy the files in php/ to <php include path>/api_producer/. E.g.:
  cp -R php /usr/local/include/php/api_producer
